#!/usr/bin/make -f

%:
	dh $@

override_dh_auto_install:
	$(MAKE) DESTDIR=$$(pwd)/debian/cvm-cli PREFIX=/usr install

override_dh_auto_build:
	# Nothing to build

override_dh_auto_test:
	$(MAKE) test
