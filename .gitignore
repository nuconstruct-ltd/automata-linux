# Disk images
azure_disk.vhd
aws_disk.vmdk
gcp_disk.tar.gz
aws-uefi-blob.bin

# Secrets & keys
*.pem
*.key
*.pub
livepatch.crt
**/secrets/*

# Workload directories (contain secrets, tars, configs)
workload-*/
workload

# Build
target/
controller/target

# Package artifacts
*.deb
*.tar
*.raw
*.zip
macos-arm64.tar.gz
secure_boot
.env

# Legacy artifacts
_artifacts/
_artifacts

# IDE / tooling
.claude
**.DS_Store
